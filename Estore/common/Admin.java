package common;

public class Admin extends Person {
    public Admin(String firstName, String lastName, String username, String password, String accessType) {
        super(firstName, lastName, username, password, accessType);
    }
}
